import theme from './theme.json';

export const { fontStyle } = theme;
export const { fontFamily } = theme;
export const { breakpoints } = theme;
export const { radius } = theme;
export const { spacing } = theme;
